/* ============================================================
   AW3 — site config.  Edit these, nothing else needed.
   ============================================================ */
window.AW3_CONFIG = {
  // Where your backend lives.
  //  • Same server as the site (recommended) → leave "" (empty).
  //  • Backend on another domain → "https://your-api.onrender.com"
  API_BASE: "",

  // Your Discord. The profile button links here.
  DISCORD_ID: "1209319851533930558",
  // Optional: a server invite like "https://discord.gg/xxxxx".
  // If set, the button uses this instead of your profile link.
  DISCORD_INVITE: "",

  // Credits — change SITE_AUTHOR to your name/handle.
  SITE_AUTHOR: "woko",
  FORMULA_AUTHORS: "spliax & xtermiz",
};
